:set -XOverloadedStrings
:set -XBangPatterns
import Data.Vector qualified as V
import Data.Vector (Vector)
import Data.Vector.Mutable qualified as MV
import Data.Vector.Unboxed qualified as VU
import Data.Vector.Unboxed qualified as U
import Data.Vector.Unboxed.Mutable qualified as VUM
import Data.Vector.Unboxed.Mutable qualified as MVec
import Data.Vector.Unboxed.Mutable qualified as UM
import Control.Arrow ((>>>))
import Control.Monad (replicateM ,replicateM_, when, forM_, forM, guard)
import Data.Char (isLower, isUpper, toLower, toUpper, digitToInt, intToDigit, ord)
import Data.List (group, elemIndex, sort, sortOn, partition, transpose, isPrefixOf, nub, isInfixOf, tails, (\\), find, mapAccumL, permutations, isSubsequenceOf, permutations, subsequences)
import Data.Ord (Down (..))
import Data.Map qualified as Map
import Data.Map.Strict qualified as Map
import Data.Maybe (fromMaybe, listToMaybe)
import Data.Ord (comparing, Down(..))
import Data.Set qualified as Set
import Debug.Trace (traceShowId)
import System.Exit (exitSuccess)
import Text.Printf (printf)
import Data.Sequence qualified as Seq
